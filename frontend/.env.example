# ARTHA frontend (Expo) — copy to frontend/.env. Only EXPO_PUBLIC_* values are bundled
# into the client, so never put secrets here.

# Base URL of the ARTHA FastAPI backend (no trailing slash, no /api suffix)
EXPO_PUBLIC_BACKEND_URL=http://localhost:8001
